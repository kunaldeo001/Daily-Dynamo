
{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "type": "object",
      "description": "Stores basic profile information for each user.",
      "properties": {
        "email": {
          "type": "string",
          "description": "The user's primary email address."
        },
        "displayName": {
          "type": "string",
          "description": "The user's public display name."
        }
      }
    },
    "Task": {
      "title": "Task",
      "type": "object",
      "description": "Represents a single task that a user plans for their day.",
      "properties": {
        "title": {
          "type": "string",
          "description": "A concise title or name for the task."
        },
        "isCompleted": {
          "type": "boolean",
          "description": "Indicates whether the task has been marked as completed."
        },
        "category": {
          "type": "string",
          "enum": ["Productive", "Self-Care", "Whimsical"],
          "description": "The category of the task to help organize into columns."
        },
        "priority": {
          "type": "string",
          "enum": ["Low", "Medium", "High"],
          "description": "The energy/priority level of the task."
        },
        "duration": {
          "type": "string",
          "description": "Estimated time for the task (e.g., 15m, 1h)."
        },
        "isMainQuest": {
          "type": "boolean",
          "description": "Whether this task is the primary goal for the day."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the task was initially created."
        },
        "ownerId": {
          "type": "string",
          "description": "The UID of the user who owns this task."
        }
      },
      "required": [
        "title",
        "isCompleted",
        "category",
        "createdAt",
        "ownerId"
      ]
    },
    "DailySpark": {
      "title": "DailySpark",
      "type": "object",
      "description": "Represents a daily inspiring, quirky, or unexpected suggestion/challenge generated by AI for the user.",
      "properties": {
        "sparkDate": {
          "type": "string",
          "format": "date",
          "description": "The specific calendar date for which this daily spark was generated."
        },
        "content": {
          "type": "string",
          "description": "The actual text content of the daily spark or challenge."
        },
        "imageUrl": {
          "type": "string",
          "description": "A data URI of an AI generated image visualizing the spark."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp indicating when the daily spark was generated."
        },
        "ownerId": {
          "type": "string",
          "description": "The UID of the user who owns this daily spark."
        }
      },
      "required": [
        "sparkDate",
        "content",
        "createdAt",
        "ownerId"
      ]
    },
    "DailyReflection": {
      "title": "Daily Reflection",
      "type": "object",
      "description": "A short note or reflection written by the user for a specific day.",
      "properties": {
        "reflectionDate": {
          "type": "string",
          "format": "date"
        },
        "note": {
          "type": "string"
        },
        "createdAt": {
          "type": "string",
          "format": "date-time"
        },
        "ownerId": {
          "type": "string"
        }
      },
      "required": ["reflectionDate", "note", "ownerId"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores basic profile information for each user."
    },
    "/users/{userId}/tasks/{taskId}": {
      "schema": {
        "$ref": "#/entities/Task"
      },
      "description": "Stores individual tasks created and managed by a specific user."
    },
    "/users/{userId}/dailySparks/{sparkId}": {
      "schema": {
        "$ref": "#/entities/DailySpark"
      },
      "description": "Stores AI-generated daily sparks or challenges provided to a specific user."
    },
    "/users/{userId}/reflections/{reflectionId}": {
      "schema": {
        "$ref": "#/entities/DailyReflection"
      },
      "description": "Stores daily reflections or notes for a specific user."
    }
  }
}
